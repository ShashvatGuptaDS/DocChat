version: '3.8'

services:
  docchat:
    build: .
    container_name: docchat
    ports:
      - "8501:8501"
    volumes:
      - ./logs:/app/logs
    env_file:
      - .env
    restart: unless-stopped
